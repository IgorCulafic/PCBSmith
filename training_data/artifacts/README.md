# Training Artifacts

Keep this directory light. Prefer references to generated artifacts under `outputs/` instead of copying large KiCad projects, SVG renders, PNG previews, Gerbers, drill files, or local model outputs here.

Small manifests are acceptable when they help make a dataset reproducible.
